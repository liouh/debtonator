$(function() {
    
    $('.fire').fire({
		speed:60,
        minPow:3,
		maxPow:10,
		gravity:0,
		flameWidth:3,
		flameHeight:0,
		fadingFlameSpeed:8,
        fireTransparency: 100,
        maxPowZone: 'right'
	});
    
});
